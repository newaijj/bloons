# Compiled binaries. Rebuild: cd tracker && swiftc -O Sources/*.swift -o tracker
/tracker/tracker
/probe/probe
/calibrate/calibrate

# Frame captures, ~4MB each and regenerated by any calibrate run.
# The paired OCR .txt dumps are small and kept.
/calibrate/out/*.png

# Run artifacts
/tracker/run.out

.DS_Store
